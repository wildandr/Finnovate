import React from 'react';
import { View, Text } from 'react-native';

const MarketScreen = () => {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center', backgroundColor: '#002351' }}>
      <Text>Market Screen</Text>
    </View>
  );
};

export default MarketScreen;